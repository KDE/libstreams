# Generated by CMake. Don't edit this file, changes will be lost.

@PACKAGE_INIT@

# Set Libstreams version
set(Libstreams_VERSION_MAJOR @STRIGI_VERSION_MAJOR@)
set(Libstreams_VERSION_MINOR @STRIGI_VERSION_MINOR@)
set(Libstreams_VERSION_PATCH @STRIGI_VERSION_PATCH@)
set(Libstreams_VERSION "${Libstreams_VERSION_MAJOR}.${Libstreams_VERSION_MINOR}.${Libstreams_VERSION_PATCH}" )

# Libstreams may also define the global strigi version
if(NOT DEFINED STRIGI_VERSION)
  set(STRIGI_VERSION_MAJOR @STRIGI_VERSION_MAJOR@)
  set(STRIGI_VERSION_MINOR @STRIGI_VERSION_MINOR@)
  set(STRIGI_VERSION_PATCH @STRIGI_VERSION_PATCH@)
  set(STRIGI_VERSION_STRING @STRIGI_VERSION_STRING@)
endif()

set_and_check(LIBSTREAMS_INCLUDE_DIRS @PACKAGE_INCLUDE_DESTINATION@)
set_and_check(LIBSTREAMS_LIBRARY_DIRS @PACKAGE_LIB_DESTINATION@)


if(NOT TARGET streams)
  get_filename_component(_currentDir "${CMAKE_CURRENT_LIST_FILE}" PATH)
  include("${_currentDir}/LibStreamsTargets.cmake")
endif()

set(LIBSTREAMS_LIBRARIES streams)